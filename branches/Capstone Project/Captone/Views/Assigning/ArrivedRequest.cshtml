@model IEnumerable<Captone.Models.Request>
@{
    ViewBag.Title = "Hàng đã đến trạm";
    Layout = "~/Views/Shared/_LayoutStaff.cshtml";
    var stationID = Convert.ToInt32(Session["StationID"]);
}
<h3 style="color: orange">Các gói hàng đã đến trạm & chờ giao</h3>
<div class="table-responsive" id="requestList">
    <table class="table-bordered table-striped table display KeyTable dataTable" id="TableRequest" aria-describedby="example_info">
        <thead>
            <tr>
                <th>Người gửi 
                </th>
                <th>Người nhận
                </th>
                <th>Điện thoại người nhận
                </th>
                <th>Ngày gửi
                </th>
                <th>Trạm gửi
                </th>
                <th>Ngày đến
                </th>
                <th>
                    <input type="checkbox" id="selectall" value="Select All" />
                </th>
                <th style="display: none"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {            
                <tr id="request-@item.RequestID">
                    <td style="display: none;" id="rqID">
                        @item.RequestID
                    </td>
                    <td>
                        @item.Username
                    </td>
                    <td>
                        @item.ReceiverName
                    </td>
                    <td>
                        @item.ReceiverPhone
                    </td>
                    <td>
                        @item.DateRequest.ToShortDateString()
                    </td>
                    <td>
                        @item.Station.StationName
                    </td>
                    <td>
                        @item.ArrivedDate.Value.ToShortDateString()
                    </td>
                    <td>
                        <input type="checkbox" name="chk" class="case" />
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <p>
        <a id="btnCheckDeli" onclick="arrivedRQ()" style="display: none" href="/Assigning/ArrivedRequest?stationID=@stationID">Kiểm hàng</a>
        <a onclick="delivered()" class="btn btn-success" href="/Assigning/FinishedRequest?stationID=@stationID">Xác nhận giao hàng</a>
    </p>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#selectall').click(function (event) {  //on click 
            if (this.checked) { // check select status
                $('.case').each(function () { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox1"  
                });
            } else {
                $('.case').each(function () { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox1"                       
                });
            }
        });
        $('#btnCheckDeli').trigger('click');
    });

    // get all arrived requests id
    function getRequests() {
        var request = [];
        $('[name="chk"]:checked').each(function () {
            var id = document.getElementById('rqID').innerText.trim();
            request.push(id);
        });
        return request;
    }

    // set delivery status to 6 - delivered
    function delivered() {
        var requests = getRequests();
        $.ajax({
            url: "/Request/UpdateStatus",
            type: "POST",
            data: JSON.stringify({ requestIDs: requests }),
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (result) {
            }
        });
    }

    // check after 2days requests delivered or not
    function autoSet() {
        var requests = getRequests();
        $.ajax({
            url: "/Request/AutoSet",
            type: "POST",
            data: JSON.stringify({ requestIDs: requests }),
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (result) {
            }
        });
    }

    function arrivedRQ() {
        $('#TableRequest input:checkbox').attr('checked', 'checked');
        autoSet();
        $('#TableRequest input:checkbox').removeAttr('checked');
        $(this).val('check all');
    }
</script>
